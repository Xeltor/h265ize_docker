FROM node:latest-alpine
MAINTAINER Xeltor

LABEL h265ize_version="bleading edge" architecture="amd64"

RUN apk add --no-cache --update-cache git ffmpeg shadow && \
    npm install Xeltor/h265ize --global --no-optional && \
    apk del git && \
    mkdir /watch && \
    rm /var/cache/apk/* && \
	groupmod -g 100 users && \
	usermod -u 99 -g 100 nobody && \
	chown -R 99:100 /h265ize

VOLUME ["/watch"]
WORKDIR /h265ize

ENTRYPOINT ["/usr/local/bin/h265ize", "--watch", "/watch"]
